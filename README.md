# Make-Automation
Automated Make.com workflow that sends personalized birthday emails to employees — pulls data from Google Sheets, fetches photos from Google Drive, generates warm messages via Groq's free LLM, and delivers a styled HTML birthday card by email. Runs on a daily schedule with duplicate-send protection.
